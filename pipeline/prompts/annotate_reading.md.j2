version: 2
为下面的{{ lang_name }}例句逐条标注振假名。

标注规则：
- 写法：汉字[读音]，只把汉字本身作为标注基底；送假名（汉字后面的假名）不进基底，
  例如：至[いた]って、思[おも]った、始[はじ]めて、落[お]としてしまった
- 只标汉字；假名、标点、『』等符号原样保留
- 读音用平假名；长音写实际假名（現状[げんじょう]，不用ー）
- 连续汉字逐字标注：無実[むじつ]、現実[げんじつ]
- 提供了书内注音参考时，以书内注音为准（作者读法优先）
- 多音字拿不准时（如「危うい」的「危」读あや），无法确定就整个词不标，宁缺毋错
- 去掉全部[读音]后的文本必须与原文一字不差

[例句列表]
{% for it in items %}
({{ it.i }}) 原文：{{ it.text }}
{% if it.book_annot %}   书内注音参考：{{ it.book_annot }}
{% endif %}
{% endfor %}

严格输出一个 JSON 对象，不要其他文字：
{"items":[{"i":0,"annot":""},{"i":1,"annot":""}]}
